# 🗺️ SEQUENTIAL UI ROADMAP - LIVE BUILD

**Ziel:** Rechte Sidebar mit Live Progress Timeline  
**Zeit:** 55-60 Minuten  
**Basis:** ChatGPT's Design-Empfehlung

---

## 📋 PHASE 1: HTML STRUCTURE (10 min)

### Checkpoint 1.1: Sidebar Container
```html
- Right sidebar div (fixed position)
- 3 Zustände: closed, half-open (320px), full-open (480px)
- Drag handle (>|<) immer sichtbar
- Z-index über Chat
```

### Checkpoint 1.2: Timeline Structure
```html
- Header: "Sequential Thinking" + Progress Bar
- Status badge (running/complete/failed)
- Steps container (scrollable)
- Step template (hidden, für JS cloning)
```

**Files to modify:**
- Create: `/adapters/Jarvis/static/js/sequential-ui.js` (new file)
- Modify: `/adapters/Jarvis/templates/index.html` (add sidebar HTML)

**Estimated:** 10 minutes

---

## 🎨 PHASE 2: CSS STYLING (15 min)

### Checkpoint 2.1: Sidebar Transitions
```css
- Smooth slide-in/out (transform, 250ms)
- 3 width states: 48px (closed), 320px (half), 480px (full)
- Backdrop blur when open (optional)
```

### Checkpoint 2.2: Timeline Styles
```css
- Vertical connector lines between steps
- Status colors (green/blue/gray/red)
- Icon styles (Lucide integration)
- Hover states
- Collapse/expand animation
```

### Checkpoint 2.3: Mobile Responsive
```css
- Mobile: fullscreen when open
- Swipe-to-close gesture
- Touch-friendly hit areas
```

**Files to modify:**
- Create: `/adapters/Jarvis/static/css/sequential-ui.css` (new file)
- Or add to existing CSS

**Estimated:** 15 minutes

---

## ⚙️ PHASE 3: JAVASCRIPT LOGIC (25 min)

### Checkpoint 3.1: Sidebar Controller (8 min)
```javascript
class SequentialSidebar {
  - init()
  - open(state = 'half')
  - close()
  - toggle()
  - handleDrag()
}
```

### Checkpoint 3.2: Timeline Renderer (8 min)
```javascript
- renderProgress(data)
- renderSteps(steps)
- updateStep(stepId, newData)
- addStep(stepData)
- animateStatus(stepId, status)
```

### Checkpoint 3.3: Integration (9 min)
```javascript
- Hook into existing sequential.js
- Auto-open on task start
- Poll /sequential/status/{id}
- Update timeline real-time
- Handle errors gracefully
```

**Files to modify:**
- Create: `/adapters/Jarvis/static/js/sequential-sidebar.js`
- Modify: `/adapters/Jarvis/static/js/sequential.js` (integration hooks)

**Estimated:** 25 minutes

---

## 🧪 PHASE 4: TESTING & POLISH (10 min)

### Checkpoint 4.1: Functionality Tests
```
- ✅ Sidebar opens/closes smoothly
- ✅ Drag handle works
- ✅ Timeline renders correctly
- ✅ Steps update in real-time
- ✅ Status icons animate
- ✅ Progress bar updates
```

### Checkpoint 4.2: Polish
```
- Smooth animations
- Proper z-index layering
- Icons load correctly
- Colors match theme
- No console errors
```

**Estimated:** 10 minutes

---

## 📊 TIMELINE BREAKDOWN:
```
Phase 1: HTML Structure       [████████░░] 10 min
Phase 2: CSS Styling          [███████████████] 15 min  
Phase 3: JavaScript Logic     [█████████████████████████] 25 min
Phase 4: Testing & Polish     [██████████] 10 min

Total: 60 minutes
```

---

## 🎯 SUCCESS CRITERIA:
```
✅ Right sidebar slides in when Sequential starts
✅ Drag handle (>|<) toggles sidebar
✅ Timeline shows steps with correct status icons
✅ Live updates during Sequential execution
✅ Progress bar updates correctly
✅ Smooth animations (no jank)
✅ Works on desktop (mobile nice-to-have)
✅ No breaking changes to existing chat
```

---

## 🚀 EXECUTION PLAN:

**Step-by-Step:**
1. Create HTML structure first (foundation)
2. Add CSS styling (make it look good)
3. Build JavaScript logic (make it work)
4. Test with real Sequential call
5. Polish animations and details

**Approach:**
- Build incrementally (test after each phase)
- Use ChatGPT's design exactly
- No over-engineering
- Focus on core functionality first

---

## 📁 FILES PLAN:
```
CREATE:
✅ /adapters/Jarvis/static/js/sequential-sidebar.js (~200 lines)
✅ /adapters/Jarvis/static/css/sequential-ui.css (~150 lines)

MODIFY:
✅ /adapters/Jarvis/templates/index.html (add sidebar HTML)
✅ /adapters/Jarvis/static/js/sequential.js (integration hooks)

BACKUP:
✅ index.html.backup_sidebar
✅ sequential.js.backup_sidebar
```

---

## ⏱️ CHECKPOINTS:
```
00:20 - Phase 1 complete (HTML)
00:35 - Phase 2 complete (CSS)
01:00 - Phase 3 complete (JavaScript)
01:10 - Phase 4 complete (Testing)

Target finish: ~01:15 AM
```

---

## 🎊 AFTER COMPLETION:
```
1. Test with real Sequential call
2. Record demo GIF (optional)
3. Document in Phase 4 docs
4. Sleep well knowing UI is DONE! 🎉
```

Ready Danny?
Soll ich mit Phase 1: HTML Structure starten? 🚀
Oder möchtest du noch was an der Roadmap ändern? 🤔
